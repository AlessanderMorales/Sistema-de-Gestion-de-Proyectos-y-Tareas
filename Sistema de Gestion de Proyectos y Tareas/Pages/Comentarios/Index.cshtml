@page
@model Sistema_de_Gestion_de_Proyectos_y_Tareas.Pages.Comentarios.IndexModel
@{
  ViewData["Title"] = "Lista de Comentarios";
}

<style>
    :root {
    --accent-red: #D32F2F;
        --accent-orange: #FF5722;
   --accent-gold: #FFD700;
    --accent-edit: #ffc107;
   --accent-view: #17a2b8;
 }

    .title-volcanic {
  background: linear-gradient(45deg, var(--accent-gold), var(--accent-orange));
  -webkit-background-clip: text;
   -webkit-text-fill-color: transparent;
  font-weight: 700;
    }

    .header-actions {
     display: flex;
     justify-content: space-between;
   align-items: center;
     margin-bottom: 2rem;
        border-bottom: 2px solid rgba(255,255,255,0.1);
     padding-bottom: 1rem;
    }

    .table-volcanic {
   background-color: transparent;
        color: #e0e0e0;
        border-collapse: separate;
    border-spacing: 0 10px;
    }

        .table-volcanic > :not(caption) > * > * {
   background-color: #3a3a40;
padding: 1.25rem;
   border-width: 0;
       vertical-align: middle;
        }

    .table-volcanic thead th {
   background-color: transparent;
        color: var(--accent-gold);
      font-size: 0.9rem;
 text-transform: uppercase;
   letter-spacing: 1px;
  border-bottom: 2px solid var(--accent-orange);
        }

        .table-volcanic tbody tr:hover {
  background-color: #4a4a50;
    }

    .btn-action {
    border-radius: 6px;
        font-weight: 500;
   padding: 5px 12px;
 transition: all 0.2s ease;
    }

   .btn-action:hover {
    transform: translateY(-1px);
   }

    .btn-view {
        background-color: var(--accent-view);
 border-color: var(--accent-view);
    color: white;
    }

    .btn-edit {
   background-color: var(--accent-edit);
    border-color: var(--accent-edit);
  color: #212529;
    }

    .btn-delete {
   background-color: var(--accent-red);
   border-color: var(--accent-red);
  color: white;
 }

.btn-volcanic-primary {
        background: linear-gradient(45deg, var(--accent-red), var(--accent-orange));
        border: none;
    color: white;
        font-weight: bold;
        padding: 10px 20px;
   border-radius: 8px;
   transition: all 0.3s ease;
    }

        .btn-volcanic-primary:hover {
    transform: translateY(-2px);
         box-shadow: 0 5px 15px rgba(255, 87, 34, 0.4);
        }

    .modal-content.volcanic-modal {
        background-color: #2c2c31;
        color: #f5f5f7;
   border: 1px solid rgba(255,255,255,0.2);
   border-radius: 15px;
    }

.volcanic-modal .modal-header {
   border-bottom: 1px solid var(--accent-orange);
    }

.volcanic-modal .modal-footer {
   border-top: 1px solid rgba(255,255,255,0.1);
    }

    .btn-volcanic-delete {
        background-color: var(--accent-red);
        border: none;
color: white;
   font-weight: bold;
     padding: 10px 20px;
      border-radius: 8px;
    }

  .btn-volcanic-secondary-modal {
   background-color: #4a4a50;
    border: none;
     color: white;
    font-weight: bold;
  padding: 10px 20px;
   border-radius: 8px;
    }

    /* Estilos para badges de Tipo (Enviado/Recibido) */
 .badge {
        font-size: 0.85rem;
     font-weight: 600;
  padding: 6px 12px;
    border-radius: 20px;
  text-transform: uppercase;
        letter-spacing: 0.5px;
    }

.badge.bg-info {
     background: linear-gradient(135deg, #2196F3, #1976D2) !important;
 color: white;
  box-shadow: 0 2px 8px rgba(33, 150, 243, 0.3);
    }

    .badge.bg-success {
   background: linear-gradient(135deg, #4CAF50, #388E3C) !important;
 color: white;
        box-shadow: 0 2px 8px rgba(76, 175, 80, 0.3);
    }
</style>

<div class="header-actions">
    <h2 class="title-volcanic m-0">@ViewData["Title"]</h2>

    <a asp-page="Create" class="btn btn-volcanic-primary">Nuevo Comentario</a>
</div>

<table class="table table-volcanic">
  <thead>
   <tr>
      @* NUEVO: Columna Tipo solo para empleados *@
            @if (User.IsInRole("Empleado"))
   {
   <th class="text-center">Tipo</th>
    }
            <th>Contenido</th>
  <th>Fecha</th>
  <th>Proyecto</th>
 <th>Tarea</th>
  <th>Autor</th>
      @* Solo el Jefe ve la columna "Dirigido a" *@
  @if (User.IsInRole("JefeDeProyecto") || User.IsInRole("SuperAdmin"))
   {
      <th>Dirigido a</th>
     }
       <th class="text-center">Acciones</th>
        </tr>
    </thead>
    <tbody>
     @foreach (var c in Model.Comentarios)
        {
  <tr>
  @* NUEVO: Columna Tipo para empleados *@
            @if (User.IsInRole("Empleado"))
         {
              <td class="text-center">
 @if (c.IdUsuario == Model.UsuarioActualId)
   {
            <span class="badge bg-info">Enviado</span>
        }
      else
             {
       <span class="badge bg-success">Recibido</span>
           }
 </td>
      }

<td>@c.Contenido</td>
      <td>@c.Fecha.ToString("dd/MM/yyyy HH:mm")</td>
              <td>@(c.Tarea != null && !string.IsNullOrEmpty(c.Tarea.ProyectoNombre) ? c.Tarea.ProyectoNombre : "-")</td>
<td>@(c.Tarea != null ? c.Tarea.Titulo : "-")</td>
<td>@(c.Usuario != null ? $"{c.Usuario.Nombres} {c.Usuario.PrimerApellido}" : "-")</td>
 
      @* Solo el Jefe ve a quién está dirigido *@
      @if (User.IsInRole("JefeDeProyecto") || User.IsInRole("SuperAdmin"))
       {
      <td>
       @if (c.Tarea != null && !string.IsNullOrEmpty(c.Tarea.UsuarioAsignadoNombre))
       {
              <span class="text-warning">@c.Tarea.UsuarioAsignadoNombre</span>
             }
      else
             {
      <span class="text-muted">Sin asignar</span>
 }
  </td>
  }
  
  <td class="text-center">
        <a asp-page="Mostrar" asp-route-id="@c.Id" class="btn btn-sm btn-view">Ver</a>

          @if (User.IsInRole("JefeDeProyecto") || User.IsInRole("SuperAdmin"))
  {
      <a asp-page="Edit" asp-route-id="@c.Id" class="btn btn-sm btn-edit">Editar</a>
    <form method="post" asp-page-handler="Delete" asp-route-id="@c.Id" style="display:inline;">
        <button type="submit" class="btn btn-sm btn-delete"
 onclick="return confirm('¿Deseas desactivar este comentario?');">
      Eliminar
         </button>
   </form>
   }
       </td>
 </tr>
        }
  </tbody>
</table>

<div class="modal fade" id="confirmDeleteModal" tabindex="-1" aria-labelledby="modalLabel" aria-hidden="true">
 <div class="modal-dialog modal-dialog-centered">
<div class="modal-content volcanic-modal">
 <div class="modal-header">
        <h5 class="modal-title" id="modalLabel">Confirmar Eliminacion</h5>
<button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
  </div>
<div class="modal-body">
            <p>Estas seguro de que deseas eliminar este comentario?</p>
 </div>
            <div class="modal-footer">
  <button type="button" class="btn btn-volcanic-secondary-modal" data-bs-dismiss="modal">Cancelar</button>
         <button type="button" class="btn-volcanic-delete" id="confirmDeleteButton">Si, Eliminar</button>
 </div>
  </div>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function () {
     var confirmDeleteModal = document.getElementById('confirmDeleteModal');
       var confirmDeleteButton = document.getElementById('confirmDeleteButton');
            var itemIdToDelete;

     confirmDeleteModal.addEventListener('show.bs.modal', function (event) {
    var button = event.relatedTarget;
     itemIdToDelete = button.getAttribute('data-item-id');
});

       confirmDeleteButton.addEventListener('click', function () {
       if (itemIdToDelete) {
  var form = document.getElementById('deleteForm-' + itemIdToDelete);
          if (form) {
         form.submit();
  }
           }
 });
 });
    </script>
}
